/**
 * BSP Bridges
 *
 * Adapters that connect existing protocols (LSP, MCP) into the BSP ecosystem.
 * "BSP becomes the meta-protocol — unifying LSP, MCP, and agent communication."
 */

export { LspBridge } from './lsp-bridge.js';
